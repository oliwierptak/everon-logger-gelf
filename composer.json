{
    "name": "everon/logger-gelf",
    "description": "Logger plugin with Gelf/Graylog handler for EveronLogger",
    "minimum-stability": "stable",
    "license": "MIT",
    "require": {
        "php": "^8",
        "everon/logger": "^4",
        "graylog2/gelf-php": "^1.7"
    },
    "require-dev": {
        "ext-json": "*",
        "phpunit/phpunit": "^9",
        "popo/generator": "^5",
        "symfony/var-dumper": "^5"
    },
    "autoload": {
        "psr-4": {
            "Everon\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "EveronLoggerTests\\Suite\\": "tests/suite/",
            "EveronLoggerTests\\Stub\\": "tests/stub/"
        }
    },
    "scripts": {
        "popo": [
            "vendor/bin/popo generate -q -c vendor/everon/logger/popo/shared-plugin.config.yml -s ./popo/everon-logger-gelf.popo.yml"
        ],
        "test": [
            "@popo",
            "vendor/bin/phpunit --testdox --no-coverage --group acceptance tests/suite  /Acceptance/"
        ],
        "tests-coverage": "vendor/bin/phpunit --testdox --group acceptance,default"
    },
    "scripts-descriptions": {
        "popo": "Generate POPO files",
        "test": "Quick tests",
        "test-ac": "Quick AC tests",
        "test-coverage": "Tests with coverage"
    }
}
